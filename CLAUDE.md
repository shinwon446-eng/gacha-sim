# CLAUDE.md - Ultra-Luxury GACHAFLIX (Production-Ready Edition)

이 문서는 Claude Code가 코드를 생성하고 리팩토링할 때 반드시 준수해야 하는 **프로젝트 전역 절대 지침서(헌법)**입니다.
모든 '데모', '프로토타입', '모의 데이터', '18+ 성인 전용' 문구를 원천 배제하고, **즉시 실서비스 운영 및 상용 배포가 가능한 완성형 플랫폼**으로 구현합니다.

---

## 1. 프로젝트 정체성 & 핵심 비즈니스 모델

- **서비스명**: GACHAFLIX (가차플릭스)
- **핵심 컨셉**: 넷플릭스 인터페이스 기반의 초호화 글로벌 럭셔리 랜덤박스 플랫폼.
- **경제학 및 수익 구조 (카지노 하우스 엣지 & 마이크로 베팅)**:
  - **진입장벽 파괴 (Micro Entry)**: 누구나 부담 없이 긁을 수 있는 **1.00 USDT(약 1,400원)** 박스부터 시작.
  - **슬롯머신형 도파민 (High Leverage)**: 단돈 1~3 USDT로 아이폰 16, 에어팟 맥스, 롤렉스 등 **수백~수천 배 잭팟** 도전.
  - **하우스 엣지(수익 모델)**: 건당 폭리를 취하는 것이 아닌, **RTP 94%~96% (하우스 엣지 4%~6%)**의 카지노 수학 모델로 회전율 극대화.
  - **바닥 가치 보장 (Floor Value Guarantee)**: 꽝이 나와도 박스 가격의 **80%~95% 가치를 보장**하여 "잃어도 손해는 고작 200~300원"이라는 안도감 부여.
- **금지 사항 (Zero Tolerance)**:
  - ❌ '데모', 'Demo Data', '프로토타입', '모의' 텍스트 사용 절대 금지 (100% 상용 라이브 서비스로 표기).
  - ❌ '18+', '성인 전용', '도박/사행성' 규제 문구 사용 금지 (건전한 럭셔리 쇼핑 및 공정 보상 플랫폼으로 포지셔닝).

---

## 2. 날것의 인간 구어체 카피라이팅 규칙 (AI 교과서 문체 퇴출)

모든 텍스트는 AI가 쓴 딱딱하고 어색한 번역투 문장을 전면 배제하고, **유저 입에 짝짝 붙고 손가락이 근질거리는 자연스러운 구어체와 강렬한 숏폼 카피**로 통일합니다.

### A. 메인 헤드라인 & 서브카피
- **메인 헤드라인**: **"1달러로 롤렉스 & 아이폰, 긁어보세요."** (2026-09-18 운영자 확정)
- **서브카피**: *"터지면 100% 진짜 내 것. 안 떠도 95% 즉시 환전!"* (2026-09-22 운영자 확정. 모바일은 문장 단위 줄바꿈 `<br className="sm:hidden" />` — H1 도 "…아이폰," / "긁어보세요." 로 끊는다)
- **원칙**: 모든 후킹 문구는 한 줄·짧게. 금액 표기는 정수면 소수점 없이(`32 USDT`, `$32`), 소수는 필요한 자리만(`0.9 USDT`, `2.65 USDT`).

### B. 행동 유도 버튼(CTA) 카피
- `지금 오픈하기` ➔ **`🔥 {가격}로 돌려보기`** (박스 가격 명시)
- `무료 체험해보기` ➔ **`손맛 보기 (무료)`**
- `구성품 확인` ➔ **`뭐 들었나 보기`**
- `즉시 판매` ➔ **`95% 즉시 회수`**
- `실물 배송 신청` ➔ **`집으로 배송`**

---

## 3. 가격 티어 및 박스 라인업 규격

| 카테고리 | 1회 오픈 가격 | 최고 당첨 상품 및 배수 | 꽝 발생 시 보장 가치 |
| :--- | :--- | :--- | :--- |
| **🔥 1달러의 행복** | **1.00 USDT** | **에어팟 맥스 / 아이폰 16 (1,000배)** | 0.85 USDT 즉시 환전 보장 |
| **⚡ 커피&버거 입문** | **3.00 ~ 5.00 USDT** | **맥북 프로 / 플레이스테이션 5 (1,200배)** | 85% ~ 90% 환전 보장 |
| **👑 럭셔리 볼트** | **20.00 ~ 50.00 USDT** | **롤렉스 서브마리너 / 골드바 (2,000배)** | 92% ~ 95% 환전 보장 |
| **🚀 사이버트럭 잭팟** | **100.00 USDT** | **테슬라 사이버트럭 (1,300배)** | 95.00 USDT 환전 보장 |

---

## 4. 메인 화면 레이아웃 & 전환율 극대화 계층 구조

```
[ 1. 최상단: 🏆 잭팟 풀 골드 뱃지 + 실시간 라이브 드랍 티커 (Live Drops Ticker) ]
  ├─ 좌측 고정: [ 🏆 JACKPOT POOL 455,714 USDT ] — 히어로에서 이사한 1줄 뱃지
  └─ 실시간 당첨 및 환전 스트림: "user***21님이 1 USDT 박스에서 에어팟 맥스 획득 (3초 전)"

[ 2. 빌보드 히어로 (50% 슬림화 & 럭셔리 림라이트) ]
  ├─ 좌측: [👑 1달러로 시작하는 명품 잭팟] 뱃지 + 구어체 H1 + 심플 가격 칩 + [1 USDT로 바로 돌려보기] / [손맛 보기]
  └─ 우측: 어두운 마스킹 없이 샴페인 골드 핀조명을 받는 고해상도 3D 플로팅 명품 컷

[ 3. 스티키 퀵 카테고리 탭 (sticky top-14, 헤더 h-14 아래에 붙음) ]
  └─ [전체] | [🔥 1달러의 행복] | [⚡ 애플&테크] | [👑 명품&시계] | [🚗 슈퍼카&골드바] — 탭 = 그리드 필터

[ 4. 박스 그리드 (상품 즉시 노출: Product-First, 탭 필터 스위칭) ]
  └─ 콤팩트한 16:9 넷플릭스 카드. 모바일 2열(한 화면 4~6개) → sm 3열 → md 4열 → xl 5열. 호버 시 1.05배 확대 + 플로팅 패널(구성품·등급 바)

[ 5. 🔥 핫 잭팟 TOP 3 & 🏆 주간 랭킹전 (§8-C, §8-E) ]
  └─ 3-Step 가이드 스트립 · 데일리 프리박스 스트립 · 3칸 통계 바(LiveCounters)는 전부 제거했다
     (데일리 프리박스는 헤더 뱃지로만 남고, 통계 바는 한 줄을 통째로 먹으며 템플릿 느낌만 줬다)

[ 6. 넷플릭스 럭셔리 푸터 (Footer) ]

[ 모바일(md 미만) 전용: 하단 고정 엄지 존 내비 h-14 ]
  └─ [🔥 홈] | [👑 1달러 잭팟] | [📦 내 보관함] | [💳 충전 (+)]  (messages/mobileNav.*)
  └─ 지갑 모달은 3탭 [USDT 입금][신용카드 결제][↗ 출금] — 모바일 헤더에는 출금 버튼이 없으므로 여기서 출금까지 닿는다
```

---

## 5. 넷플릭스 럭셔리 푸터 (Footer) 규격

- **배경**: 딥 옵시디언 다크 (`#0B0B0B`) + 상단 1px 메탈릭 헤어라인 테두리 (`border-metallic-subtle`).
- **1열 (브랜드 & 신뢰 뱃지)**:
  - GACHAFLIX 로고 + "블록체인 기반의 가장 투명한 실물 럭셔리 랜덤박스 플랫폼".
  - 지원 네트워크 및 보안 뱃지: `USDT TRC-20`, `USDT BEP-20`, `Visa / Mastercard`, `Provably Fair SHA-256`, `256-Bit SSL`.
- **2열 (간결한 3단 링크)**:
  - **서비스**: 1달러 박스, 명품 볼트, 실시간 라이브 피드, 공정성 검증기.
  - **이용 안내**: 서비스 이용약관, 개인정보처리방침, 배송 및 95% 환전 정책, 자주 묻는 질문(FAQ).
  - **고객지원**: 텔레그램 24/7 실시간 상담, 공식 디스코드, 공지 채널.
- **3열 (면책 조항 & 카피라이트)**:
  - *"GACHAFLIX는 전 세계 유저를 위한 글로벌 이커머스 랜덤박스 플랫폼입니다. 모든 개봉 결과는 조작 불가능한 SHA-256 알고리즘을 통해 투명하게 공개되며, 당첨된 상품은 100% 실물 배송 또는 즉시 현금(USDT) 환전이 보장됩니다."*
  - `© 2026 GACHAFLIX. All rights reserved.`

---

## 6. 가독성 황금비율 & 지갑/출금 규격

- **통화 표시 분리**: 숫자는 `text-2xl md:text-3xl font-bold tabular-nums`, 단위(`USDT`, `KRW`)는 `text-xs font-semibold text-neutral-400`으로 베이스라인 정렬 (모바일 줄바꿈 깨짐 방지).
- **출금(Withdrawal)**:
  - TRC-20 (수수료 1.00 USDT) / BEP-20 (수수료 0.80 USDT) 선택.
  - 출금 완료 시 실제 블록체인 거래 영수증 링크 제공:
    - **TronScan**: `https://tronscan.org/#/transaction/{txHash}`
    - **BscScan**: `https://bscscan.com/tx/{txHash}`
- **공정성 검증 (Provably Fair)**:
  - 외계어 수학 대신 1-Click 3단계 비주얼 타임라인(사전 봉인 증명 ➔ 기기 난수 결합 ➔ 구간 적중) 애니메이션 제공.
  - 실시간 USDT 출금 온체인 트랜잭션 링크 피드 및 택배사 실시간 배송조회 링크 제공.

---

## 7. 보안 규정 — 자금세탁·카드깡·삼자사기 차단 (2026-09-23 운영자 지시)

랜덤박스 플랫폼은 "입금 → 곧바로 출금"이 가능한 순간 자금세탁 통로가 된다. 두 겹으로 막는다.

### A. 100% 롤오버 (Wager Requirement) — `lib/rollover.ts`

- **규칙**: 출금은 **입금액의 100% 이상을 상자 개봉에 소진**한 뒤에만 열린다.
  `진행률 = min(100, floor(총개봉액 / max(1, 총입금액) × 100))`
- 누계는 `walletStore.totalDeposited / totalWagered` 가 `addTransaction` 에서 자동 적립한다(입금 `deposit_usdt|deposit_card`, 개봉 `open`). 환급(sellback)은 롤오버를 되돌리지 않는다.
- **입금 이력이 없는 잔액은 묶지 않는다** — `max(1, 총입금액)` 이므로 1 USDT 만 개봉해도 100%.
- 출금 탭 상단에 🛡️ AML 바(진행률 %, 총 입금/총 개봉, 잔여 개봉액, 사유)를 띄우고, 미달이면 버튼을 잠그고 경고 토스트(`withdraw.amlBlocked`)를 낸다. 충족 시 녹색 `✓ 롤오버 100% 충족 (출금 가능)`.

### A-2. 안티 그라인딩 가중치 (2단계, 2026-09-23)

바닥 환전율이 높은 상자는 손실이 거의 없어, 그 상자만 반복(그라인딩)하면 사실상 공짜로 롤오버를 채울 수 있다. 상자 위험도에 따라 인정 비율을 차등한다.

| 상자 | 기준 | 롤오버 인정 |
|---|---|---|
| 초저위험 | 최저 보장 환전율 **90% 이상** (`vault-*`, `jackpot-*` = 91.7~95%) | 개봉액의 **30%** |
| 일반(중·고위험) | 90% 미만 (`dollar-*` 85%, `starter-*` 88~88.3%) | 개봉액의 **100%** |

- 공식 `rolloverContribution = 개봉액 × (isLowRisk ? 0.3 : 1.0)`, 인정액은 거래 기록의 `rolloverUsdt` 에 남고 `walletStore.totalWagered` 에 누적된다. 배송비처럼 개봉이 아닌 지출은 `rolloverUsdt: 0`.
- **필요 롤오버는 크립토 입금 기준** — `requiredRollover(totalDepositedCrypto)`. 카드 입금분은 애초에 온체인 출금이 불가(§7-B)하므로 요구액에 넣지 않는다(persist v4 마이그레이션이 거래 기록에서 분리 복원).
- 출금 바는 [필요 롤오버 / 달성 롤오버 / 잔여 개봉액]과 30% 규칙 안내를 함께 보여준다 — 규칙을 숨기지 않는다.

### B. 자산 원천 분리 · 인벤토리 족보 추적 (Tainted Asset Tracking) — `lib/funding.ts`

카드 결제분이 "개봉 → 환전 → 코인 출금"으로 빠져나가는 카드깡·차지백 경로를 원천 차단한다.

| 항목 | 규칙 |
|---|---|
| 잔액 버킷 | `cryptoBalance`(USDT 온체인 입금분, 출금 가능) / `cardBalance`(카드 결제분, **온체인 출금 불가** — 개봉·실물 배송·카드 환불 전용). `balance` 는 둘의 합으로만 산출된다(어긋날 수 없다) |
| 개봉 차감 | `debitSplit` — 암호화폐 우선 소진, 모자라면 카드와 결합(Mixed). 합계가 모자라면 차감하지 않는다 |
| 아이템 족보 | 당첨 인스턴스에 `fundingSource: crypto \| card \| mixed` 와 `fundingRatio: {crypto, card}` 를 영구 기록 |
| 환급 귀속 | card 출처 → 100% `cardBalance`, crypto 출처 → 100% `cryptoBalance`, mixed → 비율대로 분할(반올림 오차는 카드 쪽에 몰아 합계 보존). **교차 환급 금지** |
| 출금 차감 | `debitCrypto` 만 사용 — 카드 잔액은 어떤 경로로도 온체인에 나가지 않는다 |
| 구버전 기록 | 족보가 없는 과거 아이템·잔액은 전부 `crypto` 로 간주(`normalizeRatio`), 지갑 persist v3 마이그레이션이 `balance → cryptoBalance` 로 옮긴다 |

- UI: 헤더 잔액 알약이 카드분이 있을 때 `↗ {crypto} / 💳 {card}` 로 분리 표기하고, 지갑 모달은 [출금 가능 잔액(USDT 입금분)] / [플레이·배송 전용 잔액(카드 충전분)] 2열로 보여준다.
- 함정: `UnboxingRoulette` 은 `box=null` 로 미리 마운트돼 있다 — 족보 ref 를 프롭 변화에 동기화하지 않으면 첫 마운트 값(crypto)이 박혀 카드 자금 개봉이 crypto 로 기록된다(2026-09-23 실제 발생·수정).
- 검증: `tests/funding.test.ts`, `tests/walletSplit.test.ts` 가 "카드 충전 → 개봉 → 환전 → 출금 시도" 전 구간을 막는지 확인한다.

### C. 지갑 UI 통합 (모바일 출금 누락 해소)

- 지갑 모달은 3탭 — [USDT 입금] / [신용카드 결제] / [↗ 출금]. 출금 본문은 `WithdrawTab` 하나를 출금 모달과 공유한다.
- 모바일에서도 헤더에 콤팩트 골드 [↗ 출금] 뱃지가 항상 보이고, 하단 내비 4번째는 [💳 지갑 (입출금)], 보관함 볼트 카드에는 [🚀 잔액 출금하기] 골드 버튼이 있다.
- 출금 수량 퀵 버튼 [+25%] [+50%] [전액 출금], 신청 버튼은 `🚀 {금액} 내 지갑으로 즉시 출금 신청`(골드). 누르면 1.5초 전송 준비 후 신청이 접수된다.
- **네트워크 수수료** TRC-20 1.00 / BEP-20 **0.50** USDT (2026-09-23 조정), 최소 출금 20 USDT.
- TxID·영수증은 부록 C 원칙 그대로 — live(API)가 준 실제 TxID 만 TronScan/BscScan 링크로 노출하고, 백엔드가 없으면 PENDING 에 머문다(가짜 해시 없음).

### C-2. 시작 잔고 (2026-09-23 운영자 지시)

- **신규 유저의 시작 잔액은 0 이다** (`START_BALANCE_USDT = 0`). 체험용 가상 잔고를 주지 않으며, '체험금' 류의 뱃지·안내 문구도 두지 않는다 — 출시 후 유저는 충전해야 개봉한다.
- 잔고가 부족한 상태에서 개봉을 누르면 부족 토스트와 함께 지갑 모달의 USDT 입금 탭을 바로 띄운다.
- 개발·검수용 잔액이 필요하면 그 기기의 저장소에만 넣고 배포본에는 넣지 않는다(코드 기본값은 항상 0).
- 무료 체험(손맛 보기)은 잔액을 건드리지 않는 연출이고, 전환 시 지급되는 웰컴 보너스(`WELCOME_BONUS_USDT`)는 운영자 프로모션이다 — 필요 없으면 이 상수만 0 으로 두면 된다.

### D. 입금 파이프라인 (3단계, 2026-09-23)

- **30분 입금 세션** — 주소 화면에 30:00 카운트다운. 만료되면 [🔄 새 세션 시작]으로 타이머만 되살린다(주소는 그대로).
- **72시간 유예 고지** — "30분 타이머 만료 후에도 72시간 이내에 블록체인에 도착한 정상 입금은 안전하게 계정으로 자동 처리됩니다." 거래소 전송 지연으로 유실됐다고 오해하지 않게 타이머 바로 아래에 붙인다.
- **더스트 방지** — "최소 입금액: 10 USDT (10 USDT 미만 전송 시 가스비 정책상 수기 확인이 필요합니다)" 를 입금 안내 첫 줄에 강조.
- **3D Secure 2.0** — `CheckoutRequest.requestThreeDSecure` 기본값 `"any"`(발급사가 면제해도 인증 요구). 인증 오버레이는 "3D Secure 2.0 인증 중".
- **즉각 입력 검증** — 카드 번호(Luhn+브랜드) · MM/YY · CVC 3자리 · 영문 소유자를 필드별로 입력 즉시 붉은 테두리 + 사유 문구.
- **영수증 인증 마크** — `liabilityShiftApplied()` 가 참일 때만 "3DS Verified (Liability Shift Applied)". PG 가 `threeDSecure: "authenticated"` 와 `liabilityShift: true` 를 돌려줬을 때만이며, 그 외에는 실제 상태(시도·미지원·실패·미보고)를 그대로 적는다 — 보안 인증 문구는 지어내지 않는다.

### E. 출금 부정거래 탐지 (4단계, 2026-09-23) — `lib/fraudScoring.ts`

출금 신청 순간 네 신호를 합산해 `riskScore` 를 내고, **40점 미만은 자동 승인 경로 / 40점 이상은 관리자 수동 심사**로 보낸다.

| 신호 | 조건 | 점수 |
|---|---|---|
| ① 신규 입금 | 첫 입금 후 24시간 이내 출금 | +30 |
| ② 고액 | 1회 500 USDT 초과 | +25 |
| ③ 매크로 봇 | 연속 스핀 간격 150ms 미만 (`stores/telemetryStore.ts` 가 개별 스핀 시각만 최근 60건 로컬 보관, 대량 개봉·무료 체험 제외) | +50 |
| ④ IP 불일치/VPN | 직전 입금 IP 국가 ≠ 출금 IP 국가 | +20 |

- ④ 는 **서버만 판정할 수 있는 신호**다 — 클라이언트는 IP·VPN 을 알 수 없고 알아내려 외부에 사용자 IP 를 보내지도 않는다. live 모드에서 백엔드가 `geoMismatch` 를 주면 가산하고, 없으면 미평가로 둔다(점수 0).
- **수동 심사** 판정 시 거래는 `TxStatus: "PENDING_ADMIN_REVIEW"` 로 기록되고 자동 브로드캐스트 경로를 타지 않는다. 영수증에 "보안 정책에 따라 24시간 이내 관리자 안전 승인 후 온체인 송금이 완료됩니다." 와 리스크 점수를 함께 보여 준다(근거를 숨기지 않는다).
- **서킷 브레이커** 1시간 누적 출금 ≥ **3,000 USDT** 이면 핫월렛 자동 출금을 멈추고 그 시간대의 모든 출금을 수동 승인으로 돌린다(`circuitState`). 출금 폼에 사용량 게이지와 "⛔ 서킷 브레이커 작동" 배너를 상시 노출한다.
- TxID·영수증은 부록 C 원칙 그대로 — 자동 승인이든 심사 통과든, TronScan/BscScan 링크는 백엔드가 준 실제 해시가 있을 때만 뜬다.

## 8. 메인 화면 — 타이포그래피 · 클린 쇼케이스 · 주간 랭킹전 (2026-09-23 운영자 지시)

### A. 단일 서체 Pretendard Variable

제목·본문·숫자·통화기호가 전부 한 벌이다. `tailwind.config.ts` 의 `display`/`sans` 가 같은 스택이고 `globals.css`
가 `:root` 변수 + `*` 선택자로 고정한다. Cinzel·Inter·`next/font` 구성은 제거했다(세리프 금지). 위계는 굵기(600~900)로만 만든다.

### B. 박스 카드 — 클린 럭셔리 쇼케이스 (`components/box/BoxCard.tsx`)

**사진이 상품이다.** 사진 위에 얹는 것은 `rank ≤ 3` 일 때의 **`🔥 HOT` 뱃지 하나**뿐이다. 그 외에는 사진 위에 어떤 텍스트도 없다.

- **영구 삭제**: 사진 우상단 **배율 뱃지(`×N배`)** — 하단 메타의 `최고 N배`와 중복이었다. 그리고 상단 `100% 꽝 없음 · 최소 N USDT 보장` 배너, `최소 N% 환급` 뱃지, `⚡ 전 품목 95% 즉시 정산` 배너,
  무지개 등급 바(`GameTierBar`), 호버 시 카드 밖으로 튀어나오던 대표 아이템 3D 팝아웃, 바닥 플로팅 확장 패널(구성품 3종 + 퀵 액션), 카드 틸트·1.05x 확대.
- **유지**: 여백 + 다크 비네팅, 호버 시 사진만 `scale-[1.03]` 줌 + 은은한 골드 림라이트. **시야를 가리는 팝업·돌출은 띄우지 않는다.**
- **하단 메타**: 박스명(화이트 볼드) / `1회 {가격}` / `최고 {N}배`(샴페인 골드).
- 보장 금액·환급률·RTP·정밀 확률은 **사라진 게 아니라 상세 모달과 `/fairness` 로 옮겼다** — 확률 공개 정책(부록 A)은 그대로다.
- 카드 클릭 = 상세 모달. 카드 자체에는 개봉 버튼을 두지 않는다.

### C. 주간 명예의 전당 랭킹전 (`lib/ranking.ts` · `components/home/HallOfFame.tsx`)

매주 **UTC 월요일 00:00** 초기화되는 잭팟 **배수** 랭킹전.

| 항목 | 값 |
|---|---|
| 총 상금 풀 | `WEEKLY_PRIZE_POOL_USDT = 5000` |
| 1~5위 지급 | `PRIZE_SPLIT_USDT = [2500, 1200, 600, 350, 350]` (합계가 풀과 같은지 테스트가 강제) |
| 진입 하한 | `MIN_RANK_MULTIPLE = 10` — 바닥 캐시백(≈0.95x)이 상위권을 도배하지 못하게 한다 |
| 시즌 구간 | `seasonWindow(now)` UTC 기준(타임존이 달라도 서버·클라가 같은 값 → 하이드레이션 안전) |
| 카운트다운 | `formatCountdown(ms)` → `D-2 14:22:05`. 마운트 전에는 `—`, `useEffect` 가 시작한다 |

- **상금은 연출이 아니라 운영자가 실제로 지급하는 프로모션이다.** 그 주에 운영하지 않으면 상수를 0 으로 두고, 지급 약속을 화면에 남겨 두지 않는다.
- **랭킹 줄은 실제 기록만 오른다** — live 는 API 가 준 마스킹 랭킹, 백엔드가 없으면 이 기기의 실제 당첨. 빈 순위는
  지어낸 당첨자(`CryptoWhale*** 롤렉스 14,800 USDT` 류)로 메우지 않고 **공석**으로 열어 두고 그 자리의 상금과 도전 목표 배수만 보여 준다(부록 C).
- **내 순위 바**는 랭킹에 올라 있으면 그 순위, 아니면 "순위권 밖"이라고만 적는다 — 집계 범위를 모르면서 "42위" 같은 숫자를 지어내지 않는다.
  하단 `scopeNote` 가 집계 범위를 그대로 고지한다.

### D. 히어로 (`components/home/BillboardHero.tsx`)

- **우측 비주얼 위에는 아무것도 얹지 않는다.** `🔥 LIVE BIG WIN` / `🎯 잭팟 도전 목표` 플로팅 카드는 제거했다 — 명품 컷을 가렸다.
- **잭팟 풀은 히어로를 떠났다.** `components/home/JackpotPoolBadge.tsx` 로 옮겨 최상단 티커 바 좌측에
  `[ 🏆 JACKPOT POOL 455,714 USDT ]` 1줄 골드 뱃지로 붙는다(모바일은 짧은 라벨 `hero.poolLabelShort`).
  값은 여전히 공개 라인업 박스들의 최고 상품 가치 합계이며 마운트 후 1.4초 이징으로 올라간다.
  화면의 부연 설명(`지금 열려 있는 12개 박스의…`)은 메인 카피 가독성을 위해 걷어냈지만, 정의는 뱃지의 `title`/`aria-label` 에 그대로 남긴다 — 지웠다고 정의까지 없애지는 않는다.
- 티커는 `now === null` 이어도 잭팟 풀 뱃지는 먼저 렌더한다(목록만 마운트 후 렌더).
- 우측 쇼케이스의 앰비언트 파티클은 고정 좌표 배열이다 — `Math.random()` 을 쓰면 SSR/CSR 이 갈려 하이드레이션이 깨진다.

### E. 킬러 섹션

튜토리얼 Step 1·2·3 스트립(`OnboardingStrip`)을 제거하고 ① 🔥 핫 잭팟 TOP 3(카탈로그 `popularity` 상대값으로 HOT °C·게이지)
② 🏆 주간 랭킹전(위 C)으로 대체했다. "오늘 N개 오픈" 처럼 우리가 가지고 있지 않은 수치는 쓰지 않는다.

### F. 주의

개발 서버가 떠 있는 동안 `next build` 를 돌리면 `.next` 가 덮여 dev 청크가 깨진다(`Cannot find module './vendor-chunks/*.js'`).
빌드 전에 preview 를 내린다.

## 부록 A. 계승 규범 (v2~v4 → v5)

v5 본문이 다루지 않는 항목은 이전 규범을 유지한다. 코드가 이미 이 값으로 구현되어 있다.

- **색·표면 토큰** obsidian `#0B0B0B` / canvas `#141414` / surface `#181818`, champagne `#E6CA65` / metallic `#D4AF37`, crimson `#E50914`. **등급** ROYAL `#E6CA65`(20x+) / PRESTIGE `#93C5FD`(6~20x) / EXECUTIVE `#C084FC`(2~6x) / CURATED `#94A3B8`.
- **홈 계층 (2026-09-23 정리)** 티커(좌측 🏆 잭팟 풀 뱃지) → 히어로 → **스티키 퀵 탭** → **단일 그리드(탭 필터)** → 🔥 핫 TOP 3 → 🏆 주간 랭킹전 → 인증 피드 → 푸터. 데일리 프리박스 스트립과 3칸 통계 바(LiveCounters)는 렌더하지 않는다(컴포넌트는 남아 있으나 홈에서 호출하지 않음). TOP 10 · 카테고리 캐러셀(NetflixRow) · 전체 그리드로 3중 나열하던 것을 그리드 하나로 일원화했다(세로 길이 ~10,000px → ~3,600px). **카드** 16:9 콤팩트 클린 쇼케이스 — 사진 위 뱃지는 우상단 하나뿐이고 호버 시 사진 1.03x 줌 + 골드 림라이트만 켜진다(§8-B 가 이 항목을 대체한다).
- **[전체] 탭 구역화 (2026-09-23)** 전체 탭은 12박스를 섞어 늘어놓지 않고 4대 카테고리 소제목(`sections.*`: 🔥 1달러의 행복 / ⚡ 애플 & 하이엔드 테크 / 👑 럭셔리 명품 & 스위스 워치 / 🚗 슈퍼카 & 순금 골드바 잭팟)으로 구역을 나눠 각 3박스를 보여준다 — 모바일에서 뒤쪽 카테고리가 누락된 것처럼 보이던 문제를 없앤다. 소제목 옆 가격대는 데이터에서 계산한다. 탭 바 우측에는 페이드 + `.swipe-hint` ➔ 가 더 있음을 알린다.
- **모바일 규범 (2026-09-22)** ① 헤더 `h-14` 고정, 퀵 탭 `sticky top-14 z-40 bg-obsidian/95 backdrop-blur-md`, 탭 클릭 시 `scrollIntoView({inline:"center"})` 로 중앙 정렬 + 그리드(`#boxes`, scroll-mt 118px)로 스크롤, 양끝 딤 페이드는 그 방향으로 더 스와이프할 수 있을 때만. ② `components/layout/MobileBottomNav.tsx` — `fixed bottom-0 h-14 md:hidden`, 레이아웃이 `pb-16 md:pb-0` 으로 푸터까지 보호. [💳 충전]은 홈에서 즉시 모달(`stores/uiStore.ts`), 다른 페이지에선 `/#deposit` 으로 이동해 연다. [👑 1달러 잭팟] = `/#category-dollar`(홈이 hashchange 로 탭 전환). 모바일 헤더의 텍스트 내비·충전 버튼은 `hidden md:flex`(하단 내비가 대신). ③ **position: sticky 주의** — `overflow-x: hidden` 은 `html` 에만 건다. body 나 main 에 걸면 그것이 스크롤 컨테이너가 되어 스티키 헤더·탭이 뷰포트에 붙지 않는다(실제로 그렇게 깨져 있던 것을 고침). ④ 언어별 줄바꿈은 `globals.css` 의 `html[lang]` 규칙 — ko `keep-all`(+ `overflow-wrap: break-word`; `anywhere` 는 min-content 를 바꿔 로고까지 쪼갬), en `break-word` + `-0.01em` 트래킹, zh `break-all`. ⑤ 금액 문자열은 숫자와 단위 사이가 NBSP(`formatCurrency` `NBSP`) 라 어디에 끼워도 "1 / USDT" 로 안 갈라지고, `<Money>` 는 `whitespace-nowrap tabular-nums`. 버튼 카피는 `whitespace-nowrap text-xs sm:text-sm`.
- **확률 노출 정책 (2026-09-21)** 카드·호버·상세 모달 본문에는 소수점 확률을 쓰지 않는다. 대신 [최고 N배 잭팟] · [환수율 RTP x%] · [최소 y% 환급 보장] 3수치와 게임형 등급 바(`Tier.gameLabel`: LEGENDARY=ROYAL / EPIC=PRESTIGE / RARE=EXECUTIVE / CASHBACK=CURATED — 색은 위 등급색 그대로)만 보인다. 정밀 확률표는 상세 모달의 접힌 "정밀 확률표 · Provably Fair" 섹션과 `/fairness` 에서만 연다(공개 자체는 유지).
- **룰렛 3단계 연출** Phase 1 속도 기반 모션 블러(≤14px) → Phase 2 감속 구간(55~96%)에서 ROYAL/PRESTIGE 타일 근처 0.3배속 + 골드 스파크 보더(`.reel-tension`) → Phase 3 ROYAL/PRESTIGE 적중 시 `MegaWinFX`(화이트→골드 플래시 · 충격파 · 코인 샤워 · 컨페티) + 결과 카드 3D 줌인. 감속 구간에는 `SHOWCASE_OFFSETS` 자리에 고등급 타일을 심는다 — 결과 칸은 Provably Fair 로 먼저 확정되며 연출이 바꾸지 않는다.
- **가챠 3대 연출 엔진 (2026-09-21)** 전부 연출이며 결과는 스핀 전 Provably Fair 로 확정된다(연출이 결과·확률을 바꾸지 않는다 — `tests/reel.test.ts`).
  ① 니어미스 `NEAR_MISS_RATE=0.5`: 저등급 결과일 때 2번에 1번 최상위 잭팟을 target±1 에 심고(`applyNearMiss`) 경계 1.5px 안쪽에 정지 → 마지막 0.4% 구간 0.3배속 + 인디케이터 0.4초 셰이크(`.marker-shake`) + 릴 ±1.5px 멈칫.
  ② 승급 반전 `FAKEOUT_RATE=0.5`(단건 수동 오픈·잭팟일 때): 0.5초 바닥 캐시백으로 위장(회색 플래시) → `UpgradeFX`(노이즈 글리치·화면 갈라짐·골드/보라 번개·UPGRADE! 네온) → 진짜 결과 + MegaWinFX.
  ③ 3단계 문지기 `VaultGateFX`(수동 오픈에만, 데모·오토플레이 제외): 금고 휠 잠금 해제(0.9s, 찰칵) → 문틈 아우라 블루→바이올렛→골드(0.8s) → 암전 + 심장 박동(0.42s) → 릴.
  기본 스핀 4.2s + 문지기 2.1s ≈ 7.2s, 니어미스 ≈ 8.5s.
- **인트로 없음** — (2026-09-22 운영자 지시) 스플래시를 제거했다. 접속 즉시 빌보드가 뜬다. 루트 `/` 는 인라인 스크립트 + meta refresh 로 0초 로케일 리다이렉트.
- **개봉 진입** 히어로 [🔥 1 USDT로 돌려보기]·카드 클릭·HotBoxes·랭킹전 줄은 모두 상세 모달로 직행한다(오토플레이·대량 개봉 옵션 노출). **개봉 수량 프리셋** [1개 오픈 (1 USDT)] [5연타 오픈] [10연타 오픈] [50개 대량 개봉] [100개 올인 잭팟] — 1x/5x 같은 배속 표기는 쓰지 않는다. 메인 버튼은 수량에 따라 🔥 N개 지금 개봉하기 / 🚀 50개 한 번에 대량 개봉하기 / 👑 100개 올인 잭팟 대량 개봉 (금액). `BULK_THRESHOLD=50` 이상은 릴 없이 `BulkOpenModal`(HMAC 병렬 확정 → 보관함 1회 일괄 저장 → 1.5초 고속 카운터 → 요약 그리드: 총 투입 vs 총 획득 · 순손익 카운트업, 잭팟 카드 골드 스파크 + 3D 플로팅, MegaWinFX).
- **오토플레이** 상세 모달 [−][🔄 오토플레이 N회][+] → `AutoplaySettingsModal`(10/25/50/100/∞ · ⚡ 자동 환전 · 스마트 정지: 잭팟(기본 켬)/단일 N배/손실 한도). 규칙은 `lib/autoplay.ts` 순수 함수(`stopReasonAfter`)로 테스트된다. 실행 중 릴 아래 [⏹ 정지 (남은 N회)]와 투입/획득/순손익 카운터, 스핀마다 가격 차감·잔고 부족 시 정지.
- **보관함 2단 탭** 👑 보유 중(IN_STORAGE, 기본) / ✅ 처리 완료(SOLD·SHIPPING_REQUESTED·SHIPPING). 보유 중은 즉시 환전 가능 총 가치(95%)·다중 선택 일괄 회수·카드별 [⚡ 95% USDT 즉시 회수]/[📦 우리 집으로 배송]; 처리 완료는 ✅ 환전 완료 +금액 / 🚚 배송 출발 / 📦 출고 준비 뱃지로 격리.
- **Provably Fair** `HMAC-SHA256(serverSeed, "clientSeed:nonce")` 앞 8 hex → `mod 1,000,000`. 3-Step 비주얼 검증기(`/fairness`, 결과 팝업, 보관함 카드).
- **i18n / 통화** `next-intl` ko/en/zh, `scripts/gen-messages.py`(`npx tsx scripts/dump-products.ts` 선행), 단일 통화 표기는 `<Money>`만.
- **배포** GitHub Pages 정적 export(basePath `/gacha-sim`, 브랜치 `feat/netflix-gacha-web`).

## 부록 B. 경제 모델 구현값 (v5 §1·§3 + 2026-09-21 하이브리드 리워드 개편)

`web/lib/products.ts` 빌더가 모듈 로드 시 강제한다(위반하면 throw).

| 규칙 | 값 |
|---|---|
| 가격 스펙트럼 | 1 / 3 / 5 / 20 / 25 / 30 / 50 / 100 USDT (12박스 · 4카테고리 dollar / tech / luxury / jackpot) |
| 아이템 종류 `kind` | `physical`(실물 · 배송 또는 95% 회수) · `digital`(Apple/Amazon/Steam 기프트카드 · 코드 발송 또는 95% 회수) · `cash`(USDT 즉시 캐시백 · 인스턴트 드롭 — 개봉 즉시 100% 잔액 적립, 보관함 레코드는 환전 완료 상태로 남아 공정성 검증 가능) |
| 바닥 등급 | 반드시 `cash`. 1달러 0.85 · 3달러 2.65 · 5달러 4.4 · 20달러 18.5 · 25달러 23 · 30달러 27.5 · 50달러 46 · 100달러 95 USDT (가격의 85~95%). 스티커·케이블 타이·키캡·필름 같은 저가 실물 꽝 금지(테스트로 차단) |
| 회수액 `sellValueOf` | physical/digital = 실판매가 × 0.95, cash = 100% |
| 정가 환원율 `EV/price` | [0.93, 1/0.95) — 실측 94.0~96.6% |
| 현금 환원율 `Σ p·sellValue / price` | 항상 < 1 (무차익). 실측 93.7~96.3% → 하우스 엣지 3.7~6.3% |
| 최고 배수 | 1달러 1,000~2,300x · 스타터 233~1,144x · 볼트 232~2,000x · 잭팟 1,300x |
| 금액 단위 | USDT 소수 둘째 자리 (0.85, 2.65 …) |

## 부록 C. 실행 모드와 정직성 원칙 (v5 §1 "데모 문구 금지"의 적용 범위)

v5 는 화면에서 '데모/모의' 문구를 없애라고 한다. 이 리포는 그 지시를 **문구 제거**로 이행하되, **없는 사실을 지어내지는 않는다**. 다른 사람의 당첨·출금·후기·TxID·운송장을 만들어 실제인 것처럼 보이는 것은 유저 기만이므로 하지 않는다.

- `lib/runtime.ts` — `NEXT_PUBLIC_API_BASE` 가 있으면 **live**, 없으면 **preview**. `lib/api.ts` 가 백엔드 계약(입금 주소 발급 · 출금 브로드캐스트/상태 · 출고 운송장 · 라이브/인증 피드 · 오늘 지표 · 준비금)을 정의한다.
- **티커 · 인증 피드 · 후기** — live 는 API 집계, preview 는 **이 기기의 실제 기록**만(마스킹 핸들 `u_xxxx***`). 기록이 없으면 티커는 렌더하지 않고 피드는 빈 상태를 보여준다.
- **신뢰 지표** — live 는 오늘 출고/환전/검증률, preview 는 지어낸 집계 대신 사실(공개 확률 항목 수 · 즉시 환전율 95% · 내 기록 재검증률).
- **출금** — live 는 API 가 준 상태·TxID 로 BROADCASTING → COMPLETED. preview 는 PENDING 에 머문다(가짜 TxID 없음). 안내문은 정식 카피(`withdraw.networkNote`)이며, API 연결 시 그대로 사실이 된다.
- **입금(USDT)** — 네트워크 TRC-20(수수료 0원 추천)/BEP-20/ERC-20, 플랫폼 전용 주소 + QR + 주소 복사, 충전 금액 프리셋, [⚡ 입금 전송 완료 (자동 잔고 확인)]. 주소는 **운영자가 실제 통제하는 지갑**(`NEXT_PUBLIC_DEPOSIT_TRC20/BEP20/ERC20`, `.env.example`) 또는 live API 발급분만 표시한다 — 지어낸 "실제 포맷" 주소를 화면에 두면 남의/존재하지 않는 주소로 송금이 일어나 복구 불가이므로 절대 하지 않는다. 확인 버튼은 live 에서 `POST /deposit/check` 를 폴링해 **체인에서 확인된 금액만** 반영하고, 백엔드가 없으면 "확인 대기 중"에 머문다(가짜 컨펌·가짜 입금 없음).
- **카드 결제** — Stripe 온램프 스타일 폼($20/$50/$100/$500 = 1:1 USDT, 카드 번호·브랜드 자동 표기·MM/YY·CVC·영문 소유자, 🔒 256-Bit SSL · PCI-DSS 뱃지, 3D Secure 애니메이션). 카드 원문은 화면 검증(Luhn·만료·CVC)에만 쓰고 어디로도 보내지 않으며, 승인·잔액 반영은 PG(Stripe/PortOne) 토큰화 세션이 성공했을 때만 일어난다. PG 키가 없으면 결제 버튼은 잠긴다(가짜 3DS 승인·가짜 영수증 없음). 2026-09-21 "미리보기 잔액 충전" 시뮬레이터와 mock 결제 삭제.
- **출고 운송장 / 준비금 지갑** — live(API) 또는 `NEXT_PUBLIC_RESERVE_ADDRESS` 가 있을 때만 표시.
- **화면 문구** — (2026-09-18·09-21 운영자 지시) '데모/프로토타입/모의/미리보기' 문구와 `demo/preview/mock` 메시지 키를 전부 정식 카피로 교체했고, 구 프로토타입 페이지(`/legacy`, 루트 `components/*`, `lib/config.ts`, `app/api`)는 삭제했다. 손맛 보기는 "무료 체험"으로만 표기한다(배송·환전 대상 아님을 결과 팝업에 명시).
- **티커 대체 콘텐츠** — 활동 기록이 없으면 공개 잭팟 라인업(박스 · 최고 상품 · 배수 · 확률)을 흘린다. 이는 상품 데이터의 사실이며 타인 활동이 아니다.
- **18+ / 도박 문구** — 코드베이스에 원래 없었고 추가하지도 않았다. 실제 출시 전 서비스 지역의 연령·경품 규제 검토는 운영자 책임이다.
